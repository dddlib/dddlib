Change Application wire-up to decouple that from the app somehow
Make mapper provider public and move to SDK? - it would need to be configurable on the Application.cs. - to allow, for instance, automapper.
rename all mapper classes to start with default.